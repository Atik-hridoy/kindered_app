import 'package:get/get.dart';

class HabitController extends GetxController {
  // This controller is intentionally left empty as the logic has been moved to the view
  // to keep the state management simple and contained within the widget tree.
}
